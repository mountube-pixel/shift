{
  "name": "percorso",
  "version": "0.1.0",
  "private": true,
  "description": "Gioco per tablet che insegna la logica procedurale: scrivi il programma a blocchi, il robot lo esegue su un labirinto isometrico",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
